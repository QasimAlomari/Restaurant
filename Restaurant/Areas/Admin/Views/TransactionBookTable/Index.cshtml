@model IEnumerable<Restaurant.ViewModels. TransactionBookTableViewModel>

@{
    ViewData["Title"] = "Index";
}

<div class="pagetitle">
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
            <li class="breadcrumb-item">News letter</li>
        </ol>
    </nav>
</div>
<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title d-flex justify-content-end">
                    </h5>
                    <table class="table datatable index-table text-break">
                        <thead>
                            <tr class="bg-dark text-white">
                                <th>
                                    @Html.DisplayNameFor(model => model. TransactionBookTableId)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model. TransactionBookTableFullName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model. TransactionBookTableEmail)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model. TransactionBookTableMobileNumber)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model. TransactionBookTableDate)
                                </th>
                            </tr>
                        </thead>
                        <tbody class="bg-dark-light">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item. TransactionBookTableId)
                                    </td>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item. TransactionBookTableFullName)
                                    </td>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item. TransactionBookTableEmail)
                                    </td>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item. TransactionBookTableMobileNumber)
                                    </td>
                                    <td class="align-middle">
                                        @Html.DisplayFor(modelItem => item. TransactionBookTableDate)
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
